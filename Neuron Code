inputs = [1.1,3.5,3.9] #neuron inputs from a previous neuron's outputs/layer
weights = [2.1,5.6,9.8] #

bias = 3 #every unique neuron has a unique bias

output = inputs[0]*weights[0] + inputs[1]*weights[1] + inputs[2]*weights[2] + bias
print(round(output))
